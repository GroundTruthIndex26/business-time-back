import { ContentPage } from "@/components/ContentBlocks";
import { HOW_IT_WORKS_PAGE, META } from "@/content/site";

export default function HowItWorks() {
  return <ContentPage page={HOW_IT_WORKS_PAGE} meta={META.howItWorks} />;
}
